# Copy to .env.e2e and fill in with a SEPARATE Supabase project used only for tests.
# The tests create and delete users, workspaces and documents.
E2E_SUPABASE_URL=
E2E_SUPABASE_ANON_KEY=
E2E_SUPABASE_SERVICE_ROLE_KEY=

# Voyage and Gemini keys are read from .env.local locally, and from secrets in CI
